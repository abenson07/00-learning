"use client";

/**
 * Login / session chrome is hidden. Restore when re-enabling auth:
 * - import Link, useRouter, useEffect, useState, Button, createSupabaseBrowserClient
 * - show “Log in” when !email, else email + Settings + Log out (see git history).
 */
export default function AuthNav() {
  return null;
}
